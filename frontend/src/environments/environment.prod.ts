// Filled in at deploy time — replace with your hosted backend's URL
// (e.g. https://project-next-backend.onrender.com) before building for production.
export const environment = {
  production: true,
  apiBase: 'https://YOUR-BACKEND-HOST/api',
  socketUrl: 'https://YOUR-BACKEND-HOST',
};
